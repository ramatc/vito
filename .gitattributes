# Normalize to LF in the repo and in the working tree on every platform.
# Without this, Windows checkouts get CRLF and `npm run format:check` fails
# because Prettier writes LF.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
