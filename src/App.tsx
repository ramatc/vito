/**
 * Placeholder shell. The real routed application (AppShell, routes, screens)
 * lands in a later work unit — this only proves the app boots with Tailwind.
 */
function App() {
  return (
    <main className="flex min-h-svh flex-col items-center justify-center gap-2 bg-white p-6 text-center">
      <h1 className="text-2xl font-semibold text-slate-900">Vito</h1>
      <p className="text-sm text-slate-500">Build your habits. Grow your buddy.</p>
    </main>
  )
}

export default App
